import 'package:flutter/material.dart';


class WallDetails extends StatelessWidget {
  const WallDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
